name: innovasive_flutter_beurer_ble
description: Helper to communicate Beurer devices via BLE
version: 0.2.12

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dartx: ^1.2.0
  device_info_plus: ^11.4.0
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.35.5
  permission_handler: ^12.0.0+1

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter: null
